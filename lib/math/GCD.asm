GCD:
PUSH(R1);
PUSH(R2);
MOV(R0, STARG(2)); 
MOV(R1, STARG(3)); 

GCD_LOOP:

CMP(R1,IMM(0));
JUMP_EQ(GCD_END_LOOP);
REM(R0,R1); 
MOV(R2,R0); 
MOV(R0,R1);
MOV(R1,R2);
JUMP(GCD_LOOP);

GCD_END_LOOP:
POP(R2);
POP(R1);

RETURN;